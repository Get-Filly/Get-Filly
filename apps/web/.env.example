# Kopieer dit bestand naar .env.local en vul de waardes in.
# .env.local wordt door .gitignore uitgesloten (mag nooit gecommit worden).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
